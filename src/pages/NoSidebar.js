import React from 'react';
import Navbar from '../components/Navbar';

const NoSidebar = () => {
    return (
        <div>
            <Navbar/>
            No sidebar
        </div>
    );
};

export default NoSidebar;